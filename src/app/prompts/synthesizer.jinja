Summarize the result set for a business user and optionally propose a Vega-Lite chart spec.
Return JSON with keys: message_md, rationale_summary, chart_spec (nullable).
Columns: {{ columns }}
Rows (first 20): {{ rows }}
